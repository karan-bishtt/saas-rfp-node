<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin List</title>

    <!-- Include CSS and other dependencies from the base layout -->
    <%- include('partials/base.ejs') %>
  </head>
  <body data-sidebar="dark">
    <!-- Include Header -->
    <%- include('partials/headers.ejs') %>

    <!-- Include Sidebar -->
    <%- include('partials/sidebars.ejs') %>

    <!-- Main Content Starts Here -->
    <div class="main-content">
      <div class="container">
        <div class="row">
          <div class="col">
            <div class="d-flex justify-content-between align-items-center">
              <h1 class="mb-5">Admin List</h1>
              <div
                id="notificationArea"
                class="alert"
                style="display: none"
              ></div>
            </div>
            <!-- Loading Indicator -->
            <div class="loading-indicator" id="loadingIndicator">
              Loading data, please wait...
            </div>

            <!-- No Data Available Message -->
            <div class="no-data d-none" id="noData">No data available.</div>
            <table class="table table-bordered d-none" id="adminTable">
              <thead>
                <tr>
                  <th>Name</th>
                  <th>Email</th>
                  <th>Mobile</th>
                  <th>Status</th>
                  <th>Action</th>
                </tr>
              </thead>
              <tbody>
                <!-- Rows will be dynamically added here -->
              </tbody>
            </table>
            <div
              id="pagination"
              class="d-none d-flex justify-content-center mt-3"
            >
              <!-- Pagination controls will be dynamically added here -->
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Include Footer -->
    <%- include('partials/footer.ejs') %>

    <script>
      $(document).ready(function () {
        const role = "<%= user.roles %>";
        const rowsPerPage = 5; // Number of rows per page
        let currentPage = 1; // Current page
        let admins = []; // Data storage

        // Show loading indicator
        const loadingIndicator = $("#loadingIndicator");
        const noDataMessage = $("#noData");
        const adminTable = $("#adminTable");
        const pagination = $("#pagination");

        // Fetch admin data
        function fetchAdminData() {
          loadingIndicator.removeClass("d-none");

          $.ajax({
            url: `/api/${role}/admins`, // Update with your actual API endpoint
            method: "GET",
            contentType: "application/json",
            beforeSend: function (xhr) {
              xhr.setRequestHeader(
                "Authorization",
                `Bearer ${localStorage.getItem("auth_token")}`
              );
            },
            success: function (response) {
              if (response.status) {
                admins = response.data.admins;

                if (admins.length === 0) {
                  noDataMessage.removeClass("d-none"); // Show no data message
                } else {
                  renderTable(); // Render table on load
                  renderPagination(); // Render pagination controls
                }
              }
            },
            error: function (xhr, status, error) {
              console.error("Error fetching data from API:", error);
            },
            complete: function () {
              loadingIndicator.addClass("d-none"); // Hide loading indicator after data is fetched
              if (admins.length > 0) {
                adminTable.removeClass("d-none"); // Show table
                pagination.removeClass("d-none"); // Show pagination
              }
            },
          });
        }

        // Render table data with pagination
        function renderTable() {
          const tableBody = $("#adminTable tbody");
          tableBody.empty();

          const start = (currentPage - 1) * rowsPerPage;
          const end = start + rowsPerPage;
          const paginatedData = admins.slice(start, end);

          paginatedData.forEach(function (admin) {
            const statusClass =
              admin.status === "Pending"
                ? "text-warning"
                : admin.status === "Rejected"
                ? "text-danger"
                : "text-success";
            const actionButtons =
              admin.status === "Pending"
                ? `
              <button class="btn btn-sm btn-primary change-status-approve" data-id="${admin.user_id}">Approve</button>
              <button class="btn btn-sm btn-danger change-status-reject" data-id="${admin.user_id}">Reject</button>
            `
                : "";

            const row = `
              <tr id="row-${admin.user_id}">
                <td>${admin.name}</td>
                <td>${admin.email}</td>
                <td>${admin.mobile}</td>
                <td class=${statusClass}>${admin.status || "Pending"}</td>
                <td id="action-${admin.user_id}">${actionButtons}</td>
              </tr>
            `;
            tableBody.append(row);
          });

          // Attach click event to the action buttons
          $(".change-status-approve").on("click", function () {
            const userId = $(this).data("id");
            changeStatus(userId, "Approved");
          });
          $(".change-status-reject").on("click", function () {
            const userId = $(this).data("id");
            changeStatus(userId, "Rejected");
          });
        }

        // Render pagination controls
        function renderPagination() {
          const pagination = $("#pagination");
          pagination.empty();

          const totalPages = Math.ceil(admins.length / rowsPerPage);

          for (let i = 1; i <= totalPages; i++) {
            const button = `<button class="btn btn-sm ${
              i === currentPage ? "btn-primary" : "btn-secondary"
            } mx-1 page-btn" data-page="${i}">${i}</button>`;
            pagination.append(button);
          }

          // Attach click event to pagination buttons
          $(".page-btn").on("click", function () {
            currentPage = parseInt($(this).data("page"));
            renderTable();
            renderPagination();
          });
        }

        // Function to change the status of an admin
        function changeStatus(userId, newStatus) {
          const actionCell = $(`#action-${userId}`);
          const originalButtons = actionCell.html();
          actionCell.html('<span class="loading-text">Loading...</span>');
          $.ajax({
            url: `/api/${role}/approve-admin`,
            method: "POST",
            contentType: "application/json",
            data: JSON.stringify({
              user_id: userId,
              status: newStatus,
            }),
            beforeSend: function (xhr) {
              xhr.setRequestHeader(
                "Authorization",
                `Bearer ${localStorage.getItem("auth_token")}`
              );
            },
            success: function (response) {
              if (response?.status) {
                showNotification("success", response?.message);
                const statusCell = $(`#row-${userId} td:nth-child(4)`);
                statusCell
                  .text(newStatus) // Update the text
                  .removeClass("text-warning text-danger text-success") // Remove existing classes
                  .addClass(
                    newStatus === "Pending"
                      ? "text-warning"
                      : newStatus === "Rejected"
                      ? "text-danger"
                      : "text-success"
                  );
                const adminIndex = admins.findIndex(
                  (admin) => admin.user_id === userId
                );
                if (adminIndex !== -1) {
                  admins[adminIndex].status = newStatus; // Update the status
                }

                actionCell.html("");
                renderTable();
              } else {
                showNotification(
                  "danger",
                  response.message || "An error occurred."
                );
                actionCell.html(originalButtons);
              }
            },
            error: function (xhr, status, error) {
              showNotification(
                "danger",
                xhr.responseJSON?.message || "Error: " + error
              );
              actionCell.html(originalButtons);
            },
          });
        }

        // Initial Fetch Call
        fetchAdminData();
      });

      // Function to show notification
      function showNotification(type, message) {
        const notificationArea = $("#notificationArea");
        notificationArea
          .removeClass()
          .addClass(`alert alert-${type}`)
          .text(message)
          .fadeIn();

        // Automatically hide after 3 seconds
        setTimeout(() => {
          notificationArea.fadeOut();
        }, 3000);
      }
    </script>
  </body>
</html>
